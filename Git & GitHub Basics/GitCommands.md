Always choose VsStudio as a IDE while installing GIT
Always add new file as a Python file.
$pwd
Present working directory
$ls 
It will pull list of files
$cd "Give the actual path"
$git init
Intitialized empty git repository
# This is a new file and our first commit
Save python file as Firs_File.py
$git status > It will tells us how many commits or changes are added
$git add .
$git status
$git commit -m "This is my first commit"
$git log - It will show us previous commits / Snapshots
$git checkout "Shift Insert"
$git Checkout -b > Creating a new branch
$git checkout -b branch_for_work "Shift Insert"
$git checkout master (Switching back to master)
$git checkout -b new_branch master
$git checkout branch_for_work

We want to push an existing repository from the command line
$git remote add -origin "Path of the github location ex - "https://github.com/rakeshbasanidevops/MyfirstCloudLearnings/tree/main/Git%20%26%20GitHub%20Basics"
